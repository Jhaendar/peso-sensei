// Import jest-dom matchers
import '@testing-library/jest-dom/vitest';
// or simply: import '@testing-library/jest-dom'; if the /vitest path doesn't resolve or work

// You can add other global setup here if needed in the future
// For example, mocking global objects, etc.

// Example of a global beforeAll/afterAll if needed:
// import { beforeAll, afterAll, afterEach } from 'vitest';
// beforeAll(() => {
//   console.log('Global beforeAll for tests');
// });
// afterAll(() => {
//   console.log('Global afterAll for tests');
// });
// afterEach(() => {
//   // cleanup on exiting
// });
